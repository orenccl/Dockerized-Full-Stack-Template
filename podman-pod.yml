# Save the output of this file and use kubectl create -f to import
# it into Kubernetes.
#
# Created with podman-4.6.1
apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: "2023-09-22T05:54:54Z"
  labels:
    app: Dockerized-Full-Stack-Template
  name: Dockerized-Full-Stack-Template
spec:
  containers:
  - args:
    - mongod
    image: docker.io/library/mongo:4.0-xenial
    name: db
    ports:
    - containerPort: 27017
      hostPort: 27017
    volumeMounts:
    - mountPath: /data/configdb
      name: vidlyConfig-pvc
    - mountPath: /data/db
      name: vidly-pvc
  - args:
    - npm
    - start
    env:
    - name: DB_URL
      value: mongodb://db/vidly
    image: localhost/backend:latest
    name: backend
    ports:
    - containerPort: 3001
      hostPort: 3001
    volumeMounts:
    - mountPath: /app
      name: backend
  - args:
    - npm
    - start
    image: localhost/frontend:latest
    name: frontend
    ports:
    - containerPort: 3000
      hostPort: 3000
    volumeMounts:
    - mountPath: /app
      name: frontend
  volumes:
  - name: vidlyConfig-pvc
    persistentVolumeClaim:
      claimName: vidlyConfig
  - name: vidly-pvc
    persistentVolumeClaim:
      claimName: vidly
  - name: backend
    hostPath:
      path: /home/test/Dockerized-Full-Stack-Template/backend
      type: Directory
  - name: frontend
    hostPath:
      path: /home/test/Dockerized-Full-Stack-Template/frontend
      type: Directory